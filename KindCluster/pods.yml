kind: Pod
apiVersion: v1
metadata:
  name: nginx-pod
  namespace: custom-ns-nginx
spec:
  containers:
    - name: nginx-container
      image: nginx:latest
      ports:
        - containerPort: 80



# Commands
# kubectl apply -f KindCluster/pod.yml